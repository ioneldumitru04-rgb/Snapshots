pipeline {
    agent any
    options {
        ansiColor('xterm')
    }
    stages {
        stage('Snapshot') {
            steps {
                sh 'echo Snapshot called' 
            }
        }
    }
}